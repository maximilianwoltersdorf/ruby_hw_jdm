<footer> <p align = center> created by: Julian, Dirk, Max<br />
 
</footer>
 <% if Rails.env.development? %>
  	<div style="text-align: left;">
	  	<%= debug(params) %>
	  	<%= debug(session) %>
  	</div>
  <% end %>